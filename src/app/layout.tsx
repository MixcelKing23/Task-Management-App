import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Task Manager Application",
  description: "This Task Manager is a simple yet effective web application built using React and TypeScript. It allows users to add, manage, and delete tasks efficiently. The application features an intuitive interface where users can input tasks, mark them as completed, and remove them when necessary. Built with React’s useState hook, it demonstrates key concepts such as state management, event handling, and functional components.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
